{
  "name": "pet-shop",
  "version": "1.0.0",
  "description": "",
  "main": "truffle.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "dev": "lite-server",
    "update-dev": "truffle migrate",
    "update-testnet": "truffle migrate --network ropsten",
    "reset-testnet": "truffle migrate --network ropsten --reset",
    "deploy-web": "now --prod",
    "test": "truffle test"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lite-server": "^2.3.0"
  },
  "dependencies": {
    "@truffle/hdwallet-provider": "^1.0.35",
    "dotenv": "^8.2.0"
  }
}
